@{
  Layout = "_Layout";
}

@using ToDoList.Models;
@model List<ToDoList.Models.Category>; // gives access to neccessary list of items

<h1>Categories</h1>

<ul>
  @foreach(Category category in Model)
  {
      <li>@Html.ActionLink($"{category.Name}", "Details", new { id = category.CategoryId })</li> //property names of the object we pass in {id = item.ItemId} MUST match parameter names used by the target method (Details())
  }
</ul>

@if (@Model.Count == 0)
{
  <h3>No categories have been added yet!</h3>
} 
<br>
<p>@Html.ActionLink("Add new category", "Create")</p>
